# FBX 快速预览器 v1.0.6 发布说明

## Windows 打开方式

- 安装版会将本程序注册为 Windows 的 FBX“打开方式”候选。
- 用户可以在 Windows 设置或文件右键“打开方式”中选择本程序作为 `.fbx` 默认应用。
- 程序不会绕过 Windows 保护机制强制修改 `UserChoice`；最终默认应用仍由用户确认。
- 覆盖安装会保留文件关联，真实卸载时会清理本程序写入的关联信息。
- 便携版不会写入系统文件关联。

## 外部 FBX 预览

- 双击 FBX 或通过“打开方式”启动时，默认在当前窗口临时预览，并保留正在浏览的目录。
- 如果目标模型本来就在当前目录列表中，会直接选中正常条目，不生成重复临时项。
- 列表和缩略图都会明确标记临时模型，缩略图使用小型“临时”角标。
- 偏好设置新增“外部打开”，可选择在当前窗口预览或在新窗口打开。
- 新窗口仍运行在同一个应用进程中，各窗口的目录扫描、打开队列和视口状态互不干扰。
- 优化窗口恢复与模型加载顺序，并将原生窗口背景设为视口灰色，减少切回已有窗口时出现黑帧。

## 贴图预览

- 新增固定旋转角度，适合检查指定方向的贴图效果。
- 新增独立的持续旋转速度，正负值控制方向，速度不会改变固定角度输入值。
- 自动旋转会在固定角度的基础上持续运行。
- 拖入新贴图时保留当前平铺、偏移、流动、固定角度和旋转速度等预览参数。
- 数值标签拖动不会再误触发画布的文件拖入提示。

## 界面调整

- 顶部低频操作收纳到紧凑的“更多”菜单。
- “最近目录”放在“打开文件夹”旁边，减少鼠标移动距离。
- 修复“检查更新”进行中状态破坏菜单结构或显示异常的问题。
- 贴图旋转参数沿用现有拖动、双击归零、撤销和重做交互。

## 更新说明

- 安装版 1.0.5 可在后台下载 1.0.6，下载完成后由用户选择重启安装。
- 更新会沿用原安装目录，并保留收藏、设置、UV 元数据与 `FBX_Data`。
- 便携版仍需从发布页手动下载新版。
- electron-builder 更新到 v26 维护版 `26.15.7`，并锁定安全版构建依赖；发布前 npm 审计为 0 个已知漏洞。
- 本版本支持 Windows 10 / 11，发布产物暂未进行商业代码签名，Windows 可能显示未知发布者或 SmartScreen 提示。
